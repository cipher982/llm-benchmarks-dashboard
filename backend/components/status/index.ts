/**
 * Status page components.
 *
 * The ten styled components that used to live here — a bordered container,
 * header, badge and indicator per provider section — went with the layout that
 * needed them. A section is now a single table with provider as a column.
 */

export { default as StatusTable } from './StatusTable';
